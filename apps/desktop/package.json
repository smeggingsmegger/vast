{
  "name": "@vast/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri 2 desktop shell for Vast",
  "scripts": {
    "tauri": "tauri",
    "sidecar": "bash ../../scripts/desktop-package-sidecar.sh",
    "dev": "tauri dev",
    "build": "bash ../../scripts/desktop-package-sidecar.sh && tauri build",
    "build:debug": "bash ../../scripts/desktop-package-sidecar.sh && tauri build --debug",
    "build:app": "tauri build",
    "info": "tauri info",
    "typecheck": "echo 'ok'",
    "lint": "echo 'ok'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0"
  }
}
